# Source material kept locally, deliberately not published.

# Internal end-of-co-op deck. The published Titan page is written from it, but
# the deck itself contains figures that have not been cleared for public release.
assets/documents/EndOfCoopPresentation.pptx

# Full desktop screen capture of the multi-robot simulation. Shows a teammate's
# username and home directory in the terminal pane.
assets/videos/Auto_nav_simulation.mp4
